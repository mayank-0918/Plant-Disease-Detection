services:
  - type: web
    name: plant-desease-backend
    env: python
    buildCommand: |
      pip install -r requirements.txt
      cd client
      npm install
      npm run build
    startCommand: gunicorn app:app
    envVars:
      - key: PORT
        value: 5000
